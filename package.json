{
  "name": "utub-short-kill",
  "version": "1.2.0",
  "private": true,
  "description": "Masque et protège l'accès aux YouTube Shorts (extension + userscript).",
  "type": "module",
  "scripts": {
    "build": "node build/build.mjs",
    "check": "node tests/check.mjs",
    "test": "node tests/check.mjs && node tests/extension.test.mjs && node tests/userscript.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright": "^1.48.0"
  }
}
